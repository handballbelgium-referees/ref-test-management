query getQuestionsByIdWithIsCorrectAnswers($ids: [ID!]!) {
    questionsById(ids: $ids) {
        id
        number
        phrase
        translations
        answers {
            nodes {
                id
                phrase
                translations
                isCorrect
            }
        }
    }
}