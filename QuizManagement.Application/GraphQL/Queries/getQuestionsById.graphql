query getQuestionsById($ids: [ID!]!) {
    questionsById(ids: $ids) {
        id
        phrase
        translations
        answers {
            nodes {
                id
                phrase
                translations
            }
        }
    }
}