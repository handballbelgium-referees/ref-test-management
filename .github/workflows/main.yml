name: Main CI/CD

on:
  push:
    branches: [main, beta]

env:
  DOTNET_VERSION: 10.0.x
  NODE_VERSION: 22

jobs:
  release:
    name: 🚀 Release Job
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    outputs:
      released: ${{ steps.semantic.outputs.new_release_published }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # fetch full history to read Git tags

      - name: ⚡ Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏷 Run semantic-release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Required for GitHub releases
        run: npx semantic-release

  build:
    name: 🛠 Build Job
    runs-on: ubuntu-latest
    needs: release
    if: needs.release.outputs.released == 'true'
    permissions:
      contents: read

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # needed to read Git tags

      - name: 🏷 Get version from Git tag
        run: |
          VERSION=$(git describe --tags --abbrev=0 | sed 's/^v//')
          echo "APP_VERSION=$VERSION" >> $GITHUB_ENV
          echo "Building version $VERSION"

      - name: ⚡ Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📝 Inject version into Angular
        run: |
          sed -i "s/__APP_VERSION__/${APP_VERSION}/g" QuizManagement.Ui/src/version.ts

      - name: 📦 Install Angular dependencies
        working-directory: ./QuizManagement.Ui
        run: npm ci

      - name: 🏗 Build Angular application
        working-directory: ./QuizManagement.Ui
        run: npm run build -- --configuration production

      - name: 🗂 Prepare wwwroot folder
        run: mkdir -p QuizManagement.Api/wwwroot

      - name: 📤 Copy Angular build to .NET wwwroot
        run: |
          cp -r QuizManagement.Ui/dist/QuizManagement.Ui/browser/* QuizManagement.Api/wwwroot/

      - name: ⚡ Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 🔄 Restore .NET dependencies
        run: dotnet restore handball-belgium-rules-quiz-management.sln

      - name: 🏗 Build .NET application
        run: dotnet build handball-belgium-rules-quiz-management.sln --configuration Release --no-restore

      - name: 📦 Publish .NET application
        run: |
          dotnet publish QuizManagement.Api/QuizManagement.Api.csproj \
            --configuration Release \
            --no-build \
            --output ./publish

      - name: ⬆️ Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: dist
          path: ./publish
          retention-days: 1

  deploy:
    name: 🚢 Deploy Job
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read
      actions: read    

    steps:
      - name: ⬇️ Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: ${{ github.workspace }}/publish

      - name: 🔑 Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ github.ref == 'refs/heads/main' && secrets.AZURE_WEBAPP_NAME || secrets.AZURE_WEBAPP_BETA_NAME }}
          package: ${{ github.workspace }}/publish
          clean: true
