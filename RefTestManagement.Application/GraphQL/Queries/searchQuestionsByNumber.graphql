query searchQuestionsByNumber($number: String) {
    questions(where: {number: {startsWith: $number}} order: {number: ASC}) {
        nodes {
            id
            number
            phrase
            translations
        }
    }
}